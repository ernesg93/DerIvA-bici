import type { SeoDefinition } from "@/src/core/seo/types";

export const platformSeoDefinition: SeoDefinition = {
  title: "Unificadora",
  description: "Core skeleton for a multi-brand landing platform."
};
